spring.datasource.url: jdbc:h2:file:./camunda-h2-database

camunda.bpm:
  admin-user:
    id: demo
    password: demo
  deployment-resource-pattern: classpath:/models/*.bpmn, classpath:/models/forms/*.html, classpath:/models/*.dmn
  filter:
    create: All tasks